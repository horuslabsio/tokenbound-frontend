const Spinner = () => {
  return (
    <span
      className="mx-auto inline-block h-[28px] w-[28px] animate-spin rounded-full border-2 border-deep-blue border-b-transparent"
      aria-label="Loading"
    ></span>
  );
};

export default Spinner;
